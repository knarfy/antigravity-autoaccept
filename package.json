{
    "name": "antigravity-autoaccept",
    "displayName": "Antigravity AutoAccept",
    "description": "Auto-accepts Antigravity Agent steps: edits, terminal commands, and manual confirmations using CDP physical clicks.",
    "version": "1.2.2",
    "publisher": "knarf",
    "repository": {
        "type": "git",
        "url": "https://github.com/knarfy/antigravity-autoaccept.git"
    },
    "engines": {
        "vscode": "^1.85.0"
    },
    "categories": [
        "Other"
    ],
    "keywords": [
        "antigravity",
        "autoaccept",
        "automation",
        "agent"
    ],
    "activationEvents": [
        "onStartupFinished"
    ],
    "main": "./out/extension.js",
    "contributes": {
        "commands": [
            {
                "command": "autoAccept.toggle",
                "title": "AntiGravity AutoAccept: Toggle ON/OFF"
            }
        ],
        "configuration": {
            "title": "Antigravity AutoAccept",
            "properties": {
                "autoAccept.pollInterval": {
                    "type": "number",
                    "default": 500,
                    "description": "Polling interval in milliseconds to detect buttons in the agent panel."
                },
                "autoAccept.customButtonTexts": {
                    "type": "array",
                    "default": [],
                    "items": {
                        "type": "string"
                    },
                    "description": "Additional button texts to auto-accept (case insensitive)."
                },
                "autoAccept.excludedButtonTexts": {
                    "type": "array",
                    "default": [
                        "confirmar",
                        "confirm",
                        "run and debug"
                    ],
                    "items": {
                        "type": "string"
                    },
                    "description": "Button texts that should NEVER be auto-accepted, even if they match defaults or custom entries."
                },
                "autoAccept.enableOnStartup": {
                    "type": "boolean",
                    "default": true,
                    "description": "Automatically enable AutoAccept when Antigravity starts."
                },
                "autoAccept.cdpPort": {
                    "type": "number",
                    "default": 9222,
                    "description": "Chrome DevTools Protocol (CDP) port."
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "test": "jest",
        "fix:test": "node scripts/bump_version.js --mode test",
        "fix:stable": "node scripts/bump_version.js --mode stable"
    },
    "devDependencies": {
        "@types/jest": "^29.5.12",
        "@types/node": "^18.0.0",
        "@types/vscode": "^1.85.0",
        "@types/ws": "^8.5.10",
        "jest": "^29.7.0",
        "jest-environment-jsdom": "^30.3.0",
        "ts-jest": "^29.1.5",
        "typescript": "^5.3.3"
    },
    "dependencies": {
        "ws": "^8.16.0"
    },
    "license": "MIT"
}